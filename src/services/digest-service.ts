import { createDailyDigest } from '@/workflows'

// ============================================================
// Digest Service
// Thin wrapper — all logic lives in workflows/digest-pipeline.ts
// ============================================================

export { createDailyDigest }
